---
name: purplegold-init
description: 用紫金规范（Purple Gold）初始化当前项目：复制实例模板、建立 purplegold/ 与 紫金产物/ 骨架并验收。当用户要求初始化/实例化紫金规范时使用。
---

# 紫金规范实例初始化

把紫金规范落地到当前项目中。初始化 = 将本 skill 自带的实例模板实例化到项目根，并建立空的工作文档骨架。

本 skill 是自含包：实例模板与版本号均在 skill 目录内，初始化全程离线可用，不依赖本体仓库。

## 前提与输入

- 模板与版本号来源：本 skill 目录（本 SKILL.md 所在目录）下的 `实例模板/` 与 `VERSION`。以下记作 `<skill>`。
- 项目名：若用户未提供，先问清楚再动手（根模块名 = 项目名）。
- 目标项目根 = 当前工作目录。若已存在 `purplegold/`、`紫金产物/` 或 `project/ALLOY.md`，说明可能已初始化过：先按第 7 步的判定条件检查，已初始化则停止并报告，不要覆盖。

## 步骤

以下 `<项目根>` 指当前工作目录。

1. **Git 仓库（强制要求）**：确保 `<项目根>/` 为 Git 仓库根——其下不存在 `.git/` 时执行 `git init`，已存在则跳过。Git 版本管理须覆盖整个项目根（含以下步骤创建的 `purplegold/`、`紫金产物/`、`project/` 与 `AGENTS.md`）。
2. **复制实例模板**：将 `<skill>/实例模板/purplegold/` 与 `<skill>/实例模板/紫金产物/` 两个文件夹整体复制到 `<项目根>/`（保留 `.gitkeep` 占位文件）。
3. **写入版本号**：读取 `<skill>/VERSION` 的内容，原样写入 `<项目根>/purplegold/VERSION`。
4. **创建工程根与根模块合金文档**：新建 `<项目根>/project/`；以 `<项目根>/purplegold/templates/ALLOY.root.template.md` 为底本创建 `<项目根>/project/ALLOY.md`：填写项目名（H1 命名行）、时间戳行（当前时间）、模块简介中的项目简介；删除模板中的引导注释与 `{{…}}` 占位符；无子模块时"子模块指针"小节写"（暂无子模块。）"，"内部关系"保留一个空的 `graph TD` mermaid 块，"边界关系"写"无"。
5. **AGENTS.md 指针**：`<项目根>/AGENTS.md` 不存在则创建，存在则保留原有内容、追加一行；确保其中含一行指针，大意为："本项目使用紫金规范，请先读 `purplegold/RULES.md`"。
6. **对齐与校验**：运行 `python3 purplegold/scripts/align.py` 生成根模块派生区，再运行 `python3 purplegold/scripts/validate.py`。校验必须全绿；不绿则修复后重跑。
7. **核对就位**（初始化完成的判定条件）：项目根下 `AGENTS.md`、`purplegold/`、`紫金产物/`、`project/` 四者齐全且 `.git/` 存在；`紫金产物/规范反馈.md` 保持模板骨架状态；`紫金产物/脊椎产物/` 就位且为空（含一个 `.gitkeep`；其下的阶段文件夹不预建，随首次产出创建）；`紫金产物/肋骨产物/AB03-项目最新状态/` 就位且三份状态文档（`脊椎流程进展.md`、`肋骨规范进展.md`、`外骨骼实现情况.md`）保持模板骨架状态，其余肋骨产物文件夹不预建、随首次产出创建；`purplegold/脊椎规范/` 下八个阶段文件夹（`S01-需求确认/` 至 `S08-实机测试和验收/`，含 `S05-工程架构与契约/`、`S07-开发和测试/`）各含 `规范.md`；`purplegold/肋骨规范/` 下十三份初始肋骨规范文件夹（`AF01-提示词节点/`、`AF02-需求基线/`、`AB03-项目最新状态/`、`AB04-脊椎产物变更记录/`、`AB05-需求追溯表/`、`AB06-美术素材备份/`、`M01-部署和运维/`、`M02-修复和优化/`、`M03-上架/`、`M04-工作量评估/`、`M05-甲方原始文件/`、`M06-对外文档/`、`M07-账号密码管理/`）各含 `规范.md`（`M06-对外文档/` 另含参数文件 `审阅人.txt`）。

## 完成后

向用户报告：使用的紫金规范版本（即 `<skill>/VERSION` 的内容）、创建的内容清单、validate.py 的结果。并提示下一步：脊椎流程从 `S01-需求确认` 阶段开始，`紫金产物/肋骨产物/AB03-项目最新状态/脊椎流程进展.md` 中的脊椎流程进展是整个项目的宏观待办清单。

## 维护说明（给紫金规范作者）

- 本 skill 打包内置实例模板与 `VERSION`（来源：本体仓库 `purple-gold/实例模板/` 与 `purple-gold/VERSION`）。修改实例模板或发布新规范版本后，须在本体仓库根运行 `skill/sync-skill.sh` 同步，再一并提交。
- 初始化流程本身的规定以本体 `规范正文/04-初始化规范.md` 为准；本文件是它的可执行转述，两者冲突时改本文件。
