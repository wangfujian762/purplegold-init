# 合金文档 · {{模块命名}}
> 最后修改：{{YYYY-MM-DD HH:MM:SS}}

<!--
本文件是合金文档模板（文件夹模块）。使用时复制为模块文件夹内的 ALLOY.md 并填写。
格式要点（完整规则见实例的 RULES.md 与规范正文）：

- H1 与时间戳行为固定格式；七个必备 H2 小节标题固定，脚本按标题切分。
- 只有模块才有命名：文件夹模块的命名写在自己的 H1 里；单文件模块的命名写在父模块的"子模块指针"里。
  命名应直接说明工程角色（如"令牌刷新"，而不是"模块A"）；
  同一父模块下的子模块不得重名；命名不得包含空格、"."、"/"、"\"、"-->"。
- 路径一律为项目根相对路径；以 "/" 结尾 = 文件夹模块，否则 = 单文件模块。
- 白名单制：必备小节之外可自由添加任何小节与散文，脚本忽略其不认识的内容。
-->

## 模块简介

- 模块路径：`project/{{...}}/`
- {{本模块是什么、承担什么工程角色。}}

## 子模块指针

<!-- 列出本模块的全部子模块：文件夹模块（路径以 "/" 结尾）与单文件模块（路径不以 "/" 结尾）。
     列表项格式：- `路径` · 命名 — 说明。
     普通文件与非模块文件夹不登记在任何清单中，需要说明时写入自由小节。 -->

- `project/{{...}}` · {{命名}} — {{说明}}

## 内部关系

<!-- 说明本模块各子模块之间的依赖关系；
     依赖只允许发生在同级模块之间（同一父模块的子模块之间），必须无环；
     跨层级的依赖应改为父模块之间的依赖来表达。
     只有模块才有依赖关系：某个文件或文件夹若要进入此图，必须先认定为模块。
     mermaid 图是依赖关系的唯一权威表征，节点使用子模块的命名。 -->

{{文字阐述。}}

```mermaid
graph TD
  {{成员命名}} --> {{成员命名}}
```

## 边界关系

<!-- 本模块的子模块中，哪些直接与外部世界交互——外部世界指工程模块树之外的一切
     （文件系统、网络、数据库、操作系统环境、外设、用户、外部服务等）。
     与其他模块的交互是依赖，归父模块的内部关系，不写在这里。
     边界成员即业界术语中端口/适配器、网关、防腐层所承担的角色；只有模块才能成为边界成员。
     列表项格式：- 成员命名 — 外部对象 — 交互说明。无则写"无"。
     脚本只检查格式、以及边界成员是否确实是本模块的子模块；内容是否属实无法机器检查，靠人工维护。 -->

- {{成员命名}} — {{外部对象}} — {{交互说明}}

## 父模块指针

- 父模块：`project/{{...}}/`
- 本模块与同级模块之间的依赖关系（外部关系），记载于父模块合金文档的"内部关系"一节；全局一览见根模块合金文档派生区的"内部关系总览"。

## 接口与行为约定

<!-- 模块间的调用契约：本模块作为提供方时，其他模块调用本模块必须遵守的接口与行为约定，
     由提供方模块记录在本小节。内容不限固定格式，脚本只检查本小节的存在性；
     全局一览见根模块合金文档派生区的"接口与行为约定总览"。无约定时写"（无。）"。 -->

（无。）

## 其他信息

（无。）
