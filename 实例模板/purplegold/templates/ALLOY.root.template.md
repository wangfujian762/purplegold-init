# 合金文档 · {{项目名}}
> 最后修改：{{YYYY-MM-DD HH:MM:SS}}

<!--
本文件是根模块合金文档模板。使用时复制为 project/ALLOY.md 并填写上半部分。
根模块名 = 项目名；模块路径固定为 project/。
下方标记线以下为派生区，由对齐脚本自动生成，禁止手工编辑。
-->

## 模块简介

- 模块路径：`project/`
- {{项目简介。}}

## 子模块指针

<!-- 列出根模块的全部子模块：文件夹模块（路径以 "/" 结尾）与单文件模块（路径不以 "/" 结尾）。
     列表项格式：- `路径` · 命名 — 说明。 -->

- `project/{{...}}` · {{命名}} — {{说明}}

## 内部关系

<!-- 说明各子模块之间的依赖关系；依赖只允许发生在同级模块之间，必须无环；
     跨层级的依赖应改为父模块之间的依赖来表达。
     只有模块才有依赖关系：某个文件或文件夹若要进入此图，必须先认定为模块。
     mermaid 图是依赖关系的唯一权威表征，节点使用子模块的命名。 -->

{{文字阐述。}}

```mermaid
graph TD
  {{成员命名}} --> {{成员命名}}
```

## 边界关系

<!-- 子模块中直接与外部世界（工程模块树之外的一切）交互者；只有模块才能成为边界成员。
     列表项格式：- 成员命名 — 外部对象 — 交互说明。无则写"无"。 -->

- {{成员命名}} — {{外部对象}} — {{交互说明}}

## 父模块指针

无（根模块没有父模块）。

## 接口与行为约定

<!-- 模块间的调用契约：本模块作为提供方时，其他模块调用本模块必须遵守的接口与行为约定，
     由提供方模块记录在本小节。内容不限固定格式，脚本只检查本小节的存在性；
     全局一览见下方派生区的"接口与行为约定总览"。无约定时写"（无。）"。 -->

（无。）

## 其他信息

（无。）

---

<!-- 派生区 · 以下由对齐脚本自动生成，禁止手工编辑 -->

## 全工程模块索引

<!-- 对齐脚本生成：全部层级的所有模块，按路径排序。
     列表项格式：- `路径` · 命名。以 "/" 结尾 = 文件夹模块，否则 = 单文件模块。
     文件夹模块的合金文档路径按共置规则推导（模块路径 + ALLOY.md），不重复列出。 -->

- `project/` · {{项目名}}

## 内部关系总览

<!-- 对齐脚本生成：每份合金文档的内部关系 mermaid 图副本，按模块分节（H3 标题为模块路径）。 -->

### `project/`

```mermaid
graph TD
  {{成员命名}} --> {{成员命名}}
```

## 全工程边界总览

<!-- 对齐脚本生成：聚合全部模块的"边界关系"列表项，按模块分节（H3 标题为模块路径）；
     无边界成员的模块不列出。 -->

### `project/`

- {{成员命名}} — {{外部对象}} — {{交互说明}}

## 接口与行为约定总览

<!-- 对齐脚本生成：聚合各模块"接口与行为约定"小节的实质内容，按模块分节（H3 标题为模块路径）；
     内容为空白、仅注释或仅"（无。）"的模块不列出；全部模块均无内容时显示"（各模块均无接口与行为约定内容。）"。 -->

### `project/{{...}}/`

{{该模块"接口与行为约定"小节的内容原文。}}
